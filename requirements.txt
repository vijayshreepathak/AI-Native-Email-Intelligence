# Production runtime — Python 3.12 (Render)
# No PyTorch / evaluation deps — those live in requirements-evaluation.txt

# Web API
fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
certifi>=2024.2.2
orjson==3.10.16

# Config / validation
pydantic==2.11.3
pydantic-settings==2.8.1

# LLM orchestration
langgraph==0.3.34
langchain-core==0.3.59
langchain-anthropic==0.3.12
anthropic==0.49.0
openai==1.76.0
groq==0.23.0

# Retrieval (ChromaDB DefaultEmbeddingFunction — ONNX, no torch)
chromadb==0.6.3
numpy==2.2.4

# Database (Neon PostgreSQL)
sqlalchemy==2.0.40
psycopg2-binary==2.9.10

# Auth (Clerk JWT verification)
PyJWT[crypto]==2.10.1
